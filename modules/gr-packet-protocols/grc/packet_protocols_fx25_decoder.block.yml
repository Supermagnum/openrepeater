id: packet_protocols_fx25_decoder
label: FX.25 Decoder
category: '[Packet Protocols]'
flags: [python, cpp]

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: |-
        from gnuradio import packet_protocols
    make: |-
        packet_protocols.fx25_decoder()

file_format: 1
