id: packet_protocols_il2p_decoder
label: IL2P Decoder
category: '[Packet Protocols]'
flags: [python, cpp]

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: |-
        from gnuradio import packet_protocols
    make: |-
        packet_protocols.il2p_decoder()

file_format: 1
